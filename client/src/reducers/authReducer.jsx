const authReducer = (state,action) => {
    const {type,payload} = action
    switch (type) {
        case '':
            return {

            }
            break;
    
        default:
            break;
    }
}

export {authReducer}